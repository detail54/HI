----배열, 반복출력,

명령어 입력 -> 1. 검색 2. 생성 3. 삭제

1. 입력시 -> 기존에 있던 배열 리스트 나열되게 불러올 수 있고 배열 내용 검색 가능.
1-1 -> 명령어 입력시 모든 배열+배열내용 나열.
1-2 -> 1-1 실행시에도 배열 명 입력시 그 배열만 재 출력.
1-3 -> 배열 내용 추가
1-4 -> 배열 내용 삭제

2. 생성시 -> Scanner 이용하여 새로운 값이 입력 될때는 
새로운 배열 생성 + 무한루프 생성 + 배열 내용 검색 가능하게 + (모든 검색에는 이전단계. 1,2,3 고를수 있게 돌아가기 가능)

3. 삭제시 -> 삭제할 배열 입력하여 삭제. 경고문 1회 출력.
